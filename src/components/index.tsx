import * as React from 'react';
import { render } from 'react-dom';
import UploadPhoto from './UploadPhoto';

const rootElement = document.getElementById('root');
render(<UploadPhoto />, rootElement);
